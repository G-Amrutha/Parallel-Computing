#include <stdlib.h>

int main() {
	
    int upperLimit = 100;
	
    int sum = (upperLimit * (upperLimit + 1)) / 2;

    cout<< sum <<" is the sum of integers ranging from 1 to 100";

    return 0;
}
